{
  "name": "search-files",
  "version": "0.0.5",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "release": "npm test && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags && npm publish"
  },
  "bin": {
    "searchfile": "./app.js"
  },
  "author": "lyn",
  "repository": {
    "type": "git",
    "url": "git://github.com/mostshow/search-file.git"
  },
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^5.1.1"
  }
}
